#include <stdio.h>

int main(void)
{
    int n;

    if (scanf("%d", &n) != 1) {
        printf("Error input");
        return 0;
    }

    while ((n / 10) != 0 && 1) {
        
    }
    

    return 0;
}